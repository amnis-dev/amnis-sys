import type { ButtonProps, NavbarProps, LanguageButtonProps } from '@amnis/web/ui';

export interface WebInterfaceSchema {
  button: ButtonProps;
  navbar: NavbarProps;
  languageButton: LanguageButtonProps;
}
