import React from 'react';

export interface CrystalizerProps {
  children: React.ReactNode;
}

export const Crystalizer: React.FC = () => {
  const [state] = React.useState(false);

  return (<></>);
};
